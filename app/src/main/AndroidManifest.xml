<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 Internet chalane ka license (Google Sheet me data bhejne ke liye) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 1. Session complete hone par halki vibration ki permission -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 2. Background me pehredaar (Service) chalane ki permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 3. Lock screen par timer ka notification dikhane ki permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 4. ⏰ Thik tay samay (5:30 AM, 10:00 AM) par alarm jagane ki permission -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Hamari main screen (MainActivity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background me timer chalane wala pehredaar (TimerService) -->
        <service
            android:name=".TimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Focus Timer background countdown" />
        </service>

        <!-- 📬 Amon Reminder Receiver: Jo tay samay par notification girayega -->
        <receiver
            android:name=".AmonReminderReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
